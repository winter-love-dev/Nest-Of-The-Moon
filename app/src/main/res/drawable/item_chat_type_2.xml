<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 테두리 내부 색상 설정 -->
    <solid
        android:color="@color/bg_main_2"
        />

    <!-- 테두리 내부 여백 설정 -->
    <padding
    android:bottom="4dp"
    android:left="8dp"
    android:right="8dp"
    android:top="4dp" />

    <corners
        android:radius="10dp"
        />

<!--    <stroke
        android:width="0.5dp"
        android:color="@color/bg_main" />-->

</shape>